const backend=process.env.BACKEND_API_URL||process.env.NEXT_PUBLIC_API_URL||"http://localhost:8080";
export async function POST(_:Request,{params}:{params:Promise<{token:string}>}){const{token}=await params;if(!/^[0-9a-f-]{36}$/i.test(token))return Response.json({message:"Invalid unsubscribe link."},{status:400});try{const response=await fetch(`${backend}/api/v1/newsletter/unsubscribe/${token}`,{method:"POST",signal:AbortSignal.timeout(10000)});return new Response(response.body,{status:response.status});}catch{return Response.json({message:"Unable to unsubscribe right now."},{status:503});}}
