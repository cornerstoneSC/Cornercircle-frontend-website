"use client";
import{useState}from"react";import Link from"next/link";
export default function UnsubscribeCard({token}:{token:string}){const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);async function unsubscribe(){setBusy(true);const response=await fetch(`/api/newsletter/unsubscribe/${token}`,{method:"POST"});setStatus(response.ok?"You have been unsubscribed.":"This unsubscribe link is invalid or unavailable.");setBusy(false);}return <main className="grid min-h-[70vh] place-items-center bg-[#f7f3ec] p-6"><section className="w-full max-w-lg border border-[#b88935]/40 bg-[#fffdf9] p-10 text-center shadow-xl"><p className="text-xs font-semibold uppercase tracking-[.22em] text-[#a17834]">Cornerstone Social Circle</p><h1 className="mt-5 font-serif text-4xl text-stone-900">Newsletter preferences</h1>{status?<p role="status" className="mt-5 text-stone-600">{status}</p>:<><p className="mt-4 text-stone-600">Would you like to stop receiving newsletter updates?</p><button disabled={busy||!/^[0-9a-f-]{36}$/i.test(token)} onClick={()=>void unsubscribe()} className="mt-7 bg-[#657260] px-6 py-3 font-semibold text-white disabled:opacity-40">{busy?"Updating…":"Unsubscribe"}</button></>}<Link href="/" className="mt-7 block text-sm text-[#9c7127] underline underline-offset-4">Return home</Link></section></main>}
