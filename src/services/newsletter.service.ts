export type NewsletterSubscriber={id:number;email:string;status:"ACTIVE"|"UNSUBSCRIBED";subscribedAt:string;unsubscribedAt:string|null;unsubscribeToken:string};
export async function subscribeNewsletter(email:string,website=""){const response=await fetch("/api/newsletter/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,website})});if(!response.ok){const body=await response.json().catch(()=>null) as {message?:string}|null;throw new Error(body?.message||"Unable to subscribe right now.");}}
export async function getNewsletterSubscribers(query="",status=""){const params=new URLSearchParams();if(query)params.set("query",query);if(status)params.set("status",status);const response=await fetch(`/api/admin/newsletter?${params}`,{cache:"no-store"});if(!response.ok){const body=await response.json().catch(()=>null) as {message?:string}|null;throw new Error(body?.message||"Unable to load subscribers.");}return response.json() as Promise<NewsletterSubscriber[]>;}
